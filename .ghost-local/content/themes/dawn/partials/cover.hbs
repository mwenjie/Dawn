<div class="cover gh-outer">
    <div class="cover-content gh-inner">
        {{#if @site.icon}}
        <div class="cover-icon">
            <img class="cover-icon-image" src="{{@site.icon}}" alt="{{@site.title}}">
        </div>
        {{/if}}

        <header class="cover-header">
            <h1 class="cover-title">Building systems that survive real life</h1>
            <p class="cover-subtitle">Data engineering, AI, and infrastructure — learned the hard way.</p>
        </header>

        <div class="cover-body">
            <h2 class="cover-section-title">Why This Blog Exists</h2>

            <p>Most architecture looks clean on a slide deck.<br>Most systems fail at 3 AM.</p>

            <p>This blog is a record of what actually worked — and what didn’t — while building data platforms, AI
                workflows, and infrastructure that had to survive production, scale, and human error.</p>

            <p>I write for engineers who:</p>

            <ul class="cover-list">
                <li>Prefer clarity over hype</li>
                <li>Care about blast radius</li>
                <li>Have broken things and want to break fewer next time</li>
            </ul>
        </div>

        {{#if @site.members_enabled}}
        {{#unless @member}}
        <div class="cover-cta">
            <button class="button members-subscribe" data-portal="signup">Subscribe now</button>
            <button class="button button-secondary members-login" data-portal="signin">Login</button>
        </div>
        {{/unless}}
        {{/if}}
    </div>
</div>