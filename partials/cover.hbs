<div class="cover gh-outer">
    <div class="cover-content gh-inner">
        <div class="cover-body">
            <div class="cover-hero-grid">

                <div class="cover-hero-media">
                    <img class="cover-hero-image"
                        src="{{#if @custom.hero_image}}{{@custom.hero_image}}{{else if @site.cover_image}}{{@site.cover_image}}{{else}}https://images.unsplash.com/photo-1497032628192-86f99bcd76bc?w=1920&q=80{{/if}}"
                        alt="{{@site.title}}" loading="lazy">
                </div>

                <div class="cover-hero-content">
                    {{#if @site.icon}}
                    <div class="cover-icon cover-hero-icon">
                        <img class="cover-icon-image" src="{{@site.icon}}" alt="{{@site.title}}">
                    </div>
                    {{/if}}

                    <div class="cover-hero-copy">
                        <h1 class="cover-title">Building systems that survive real life</h1>
                        <p class="cover-subtitle">Data engineering, AI, and infrastructure — learned the hard way.</p>
                    </div>

                    <div class="cover-hero-actions">
                        {{#if @site.members_enabled}}
                        {{#unless @member}}
                        <button class="button cover-hero-button cover-hero-button-primary members-subscribe"
                            data-portal="signup">Subscribe now</button>
                        <button class="button cover-hero-button button-secondary members-login"
                            data-portal="signin">Login</button>
                        {{/unless}}
                        {{/if}}

                        <div class="cover-hero-social" aria-label="Social links">
                            {{#if @custom.hero_instagram}}
                            <a class="cover-hero-social-link" href="{{@custom.hero_instagram}}" target="_blank"
                                rel="noopener noreferrer" aria-label="Instagram">
                                {{> "icons/instagram"}}
                            </a>
                            {{/if}}
                            {{#if @custom.hero_facebook}}
                            <a class="cover-hero-social-link" href="{{@custom.hero_facebook}}" target="_blank"
                                rel="noopener noreferrer" aria-label="Facebook">
                                {{> "icons/facebook"}}
                            </a>
                            {{/if}}
                            {{#if @custom.hero_linkedin}}
                            <a class="cover-hero-social-link" href="{{@custom.hero_linkedin}}" target="_blank"
                                rel="noopener noreferrer" aria-label="LinkedIn">
                                {{> "icons/linkedin"}}
                            </a>
                            {{/if}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>